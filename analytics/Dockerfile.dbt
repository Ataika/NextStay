FROM ghcr.io/dbt-labs/dbt-postgres:1.7.0

WORKDIR /usr/app

# Сначала просто копируем файлы
COPY dbt/ .

# Не запускаем dbt deps при сборке (сделаем это позже вручную),
# чтобы билд не падал из-за отсутствия интернета или мелких ошибок.
