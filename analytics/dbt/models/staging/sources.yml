version: 2

sources:
  - name: oltp
    description: "Live OLTP source-of-truth tables (PostgreSQL prod target)."
    schema: public
    loaded_at_field: created_at
    freshness:
      warn_after: { count: 24, period: hour }
      error_after: { count: 72, period: hour }
    tables:
      - name: rooms
      - name: bookings
      - name: hotels
