services:
  backend:
    volumes:
      - ./backend:/app
    environment:
      - DEBUG=True
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

  db:
    command: ["postgres", "-c", "log_statement=all"]
